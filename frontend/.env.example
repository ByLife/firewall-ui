# Firewall UI Linux - Environment Variables
# Copy this file to .env and modify as needed

# API URL for the frontend to connect to backend
# Leave empty if frontend is served from same origin (via proxy)
# Set to http://localhost:8000 if running separately
VITE_API_URL=
